import { r as __toESM } from "./chunk-CYJPkc-J.js";
import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
import { t as require_prop_types } from "./prop-types-BN7XkUS9.js";
//#region node_modules/react-csv/src/core.js
var import_react = /* @__PURE__ */ __toESM(require_react());
/**
* Simple safari detection based on user agent test
*/
var isSafari = () => /^((?!chrome|android).)*safari/i.test(navigator.userAgent);
var isJsons = ((array) => Array.isArray(array) && array.every((row) => typeof row === "object" && !(row instanceof Array)));
var isArrays = ((array) => Array.isArray(array) && array.every((row) => Array.isArray(row)));
var jsonsHeaders = ((array) => Array.from(array.map((json) => Object.keys(json)).reduce((a, b) => new Set([...a, ...b]), [])));
var jsons2arrays = (jsons, headers) => {
	headers = headers || jsonsHeaders(jsons);
	let headerLabels = headers;
	let headerKeys = headers;
	if (isJsons(headers)) {
		headerLabels = headers.map((header) => header.label);
		headerKeys = headers.map((header) => header.key);
	}
	const data = jsons.map((object) => headerKeys.map((header) => getHeaderValue(header, object)));
	return [headerLabels, ...data];
};
var getHeaderValue = (property, obj) => {
	const foundValue = property.replace(/\[([^\]]+)]/g, ".$1").split(".").reduce(function(o, p, i, arr) {
		const value = o[p];
		if (value === void 0 || value === null) arr.splice(1);
		else return value;
	}, obj);
	return foundValue === void 0 ? property in obj ? obj[property] : "" : foundValue;
};
var elementOrEmpty = (element) => typeof element === "undefined" || element === null ? "" : element;
var joiner = ((data, separator = ",", enclosingCharacter = "\"") => {
	return data.filter((e) => e).map((row) => row.map((element) => elementOrEmpty(element)).map((column) => `${enclosingCharacter}${column}${enclosingCharacter}`).join(separator)).join(`\n`);
});
var arrays2csv = ((data, headers, separator, enclosingCharacter) => joiner(headers ? [headers, ...data] : data, separator, enclosingCharacter));
var jsons2csv = ((data, headers, separator, enclosingCharacter) => joiner(jsons2arrays(data, headers), separator, enclosingCharacter));
var string2csv = ((data, headers, separator, enclosingCharacter) => headers ? `${headers.join(separator)}\n${data}` : data.replace(/"/g, "\"\""));
var toCSV = (data, headers, separator, enclosingCharacter) => {
	if (isJsons(data)) return jsons2csv(data, headers, separator, enclosingCharacter);
	if (isArrays(data)) return arrays2csv(data, headers, separator, enclosingCharacter);
	if (typeof data === "string") return string2csv(data, headers, separator);
	throw new TypeError(`Data should be a "String", "Array of arrays" OR "Array of objects" `);
};
var buildURI = ((data, uFEFF, headers, separator, enclosingCharacter) => {
	const csv = toCSV(data, headers, separator, enclosingCharacter);
	const type = isSafari() ? "application/csv" : "text/csv";
	const blob = new Blob([uFEFF ? "﻿" : "", csv], { type });
	const dataURI = `data:${type};charset=utf-8,${uFEFF ? "﻿" : ""}${csv}`;
	const URL = window.URL || window.webkitURL;
	return typeof URL.createObjectURL === "undefined" ? dataURI : URL.createObjectURL(blob);
});
//#endregion
//#region node_modules/react-csv/src/metaProps.js
var import_prop_types = require_prop_types();
var propTypes = {
	data: (0, import_prop_types.oneOfType)([
		import_prop_types.string,
		import_prop_types.array,
		import_prop_types.func
	]).isRequired,
	headers: import_prop_types.array,
	target: import_prop_types.string,
	separator: import_prop_types.string,
	filename: import_prop_types.string,
	uFEFF: import_prop_types.bool,
	onClick: import_prop_types.func,
	asyncOnClick: import_prop_types.bool,
	enclosingCharacter: import_prop_types.string
};
var defaultProps$1 = {
	separator: ",",
	filename: "generatedBy_react-csv.csv",
	uFEFF: true,
	asyncOnClick: false,
	enclosingCharacter: "\""
};
//#endregion
//#region node_modules/react-csv/src/components/Download.js
var defaultProps = { target: "_blank" };
/**
*
* @example ../../sample-site/csvdownload.example.md
*/
var CSVDownload$1 = class extends import_react.Component {
	static defaultProps = Object.assign(defaultProps$1, defaultProps);
	static propTypes = propTypes;
	constructor(props) {
		super(props);
		this.state = {};
	}
	buildURI() {
		return buildURI(...arguments);
	}
	componentDidMount() {
		const { data, headers, separator, enclosingCharacter, uFEFF, target, specs, replace } = this.props;
		this.state.page = window.open(this.buildURI(data, uFEFF, headers, separator, enclosingCharacter), target, specs, replace);
	}
	getWindow() {
		return this.state.page;
	}
	render() {
		return null;
	}
};
//#endregion
//#region node_modules/react-csv/src/components/Link.jsx
var import_jsx_runtime = require_jsx_runtime();
/**
*
* @example ../../sample-site/csvlink.example.md
*/
var CSVLink$1 = class extends import_react.Component {
	static defaultProps = defaultProps$1;
	static propTypes = propTypes;
	constructor(props) {
		super(props);
		this.buildURI = this.buildURI.bind(this);
	}
	buildURI() {
		return buildURI(...arguments);
	}
	/**
	* In IE11 this method will trigger the file download
	*/
	handleLegacy(event, isAsync = false) {
		if (window.navigator.msSaveOrOpenBlob) {
			event.preventDefault();
			const { data, headers, separator, filename, enclosingCharacter, uFEFF } = this.props;
			const csvData = isAsync && typeof data === "function" ? data() : data;
			let blob = new Blob([uFEFF ? "﻿" : "", toCSV(csvData, headers, separator, enclosingCharacter)]);
			window.navigator.msSaveBlob(blob, filename);
			return false;
		}
	}
	handleAsyncClick(event) {
		const done = (proceed) => {
			if (proceed === false) {
				event.preventDefault();
				return;
			}
			this.handleLegacy(event, true);
		};
		this.props.onClick(event, done);
	}
	handleSyncClick(event) {
		if (this.props.onClick(event) === false) {
			event.preventDefault();
			return;
		}
		this.handleLegacy(event);
	}
	handleClick() {
		return (event) => {
			if (typeof this.props.onClick === "function") return this.props.asyncOnClick ? this.handleAsyncClick(event) : this.handleSyncClick(event);
			this.handleLegacy(event);
		};
	}
	render() {
		const { data, headers, separator, filename, uFEFF, children, onClick, asyncOnClick, enclosingCharacter, ...rest } = this.props;
		const href = typeof window === "undefined" ? "" : this.buildURI(data, uFEFF, headers, separator, enclosingCharacter);
		return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", {
			download: filename,
			...rest,
			ref: (link) => this.link = link,
			target: "_self",
			href,
			onClick: this.handleClick(),
			children
		});
	}
};
//#endregion
//#region node_modules/react-csv/src/index.js
var CSVDownload = CSVDownload$1;
var CSVLink = CSVLink$1;
//#endregion
export { CSVDownload, CSVLink };

//# sourceMappingURL=react-csv.js.map